// Lattice variables
lat:dim_t = 16
lat:dim_z = 8
lat:dim_y = 8
lat:dim_x = 8

// MP variables
mp:threads = 0
mp:np_t = 1
mp:np_z = 1
mp:np_y = 1
mp:np_x = 1

// Logger variables
log:level = 30

// Random variables
rand:seed = 32244

// Meson variables
mes:mass = -0.750
mes:csw = 1.0
mes:hits = 1
mes:prec = 1e-16
mes:method = 1

// Boundary variables
bc:cf = 1.0
